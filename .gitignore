# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
*.log
.pytest_cache/

# Node
node_modules/
dist/
dist-ssr/
*.local

# Env files (never commit secrets)
.env
.env.*
!.env.example

# Editors / OS / local tooling
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
.claude/

# Large / generated ML artifacts (regenerate via scripts/train_model.py)
# Anchored to repo root (leading /) so these don't accidentally shadow
# same-named directories elsewhere, e.g. backend/app/models/.
/models/
/catboost_info/
/artifacts/*.pkl
/Employee_Salary_Estimator_Dataset_100000 copy.xlsx

# Jupyter
.ipynb_checkpoints/
